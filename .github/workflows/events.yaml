name: Repository Events
on: 
  push:
  pull_request:
    type: [opened, assigned, reopened, synchronize ] 
  issues:
    types: [opened]

jobs:
  Checkout-test:
    runs-on: ubuntu-latest
    steps:
      - run: |
        echo $GITHUB_SHA
        echo $GITHUB_REF
      - uses: actions/checkout@v3

      

